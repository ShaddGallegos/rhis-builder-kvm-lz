# rhis-builder-kvm-lz — Checklist

Purpose: Container images and build tooling for RHIS libvirt/KVM builder, including Node components.

Prerequisites
- `podman` or `docker`, `make`

Quick start
```bash
cd rhis-builder-kvm-lz
make build
# or follow README build steps
```

Verify
- Check `container/` image manifests and `node_modules` for frontend builds

Notes
- Follow `README.md` for full build and deployment instructions.
